#include <glm/glm.hpp>

#pragma once
namespace Constants
{
    inline const glm::vec3 upVector(0.f, 1.f, 0.f);

    inline const float cameraRotSpeed = 0.02f;
    inline const float cameraMoveSpeed = 0.03f;
}